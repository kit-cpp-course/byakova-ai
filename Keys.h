#pragma once
#include "stdafx.h"

class Display /* Class needed to display keys in the files */
{
public:

	/* functiom needed to save encryption keys in the file */
	void enKey();

	/* function needed to save decryption keys in the file */
	void deKey();
};